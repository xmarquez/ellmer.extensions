% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openai-costs.R
\name{openai_costs}
\alias{openai_costs}
\title{Report OpenAI API Costs}
\usage{
openai_costs(
  start_time,
  end_time = NULL,
  api_key_ids = NULL,
  project_ids = NULL,
  group_by = NULL,
  admin_key = Sys.getenv("OPENAI_ADMIN_KEY"),
  limit = 180L,
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{start_time}{Start of the reporting period, inclusive. A \link{Date},
\link{POSIXt}, or Unix timestamp in seconds.}

\item{end_time}{End of the reporting period, exclusive. A \link{Date}, \link{POSIXt},
Unix timestamp in seconds, or \code{NULL} to use the API default.}

\item{api_key_ids}{Optional character vector of OpenAI API key IDs. These are
identifiers such as \code{"key_..."}, not secret \code{"sk-..."} key values.}

\item{project_ids}{Optional character vector of OpenAI project IDs.}

\item{group_by}{Optional character vector containing any of
\code{"api_key_id"}, \code{"project_id"}, and \code{"line_item"}.}

\item{admin_key}{An OpenAI Organization Admin API key. Defaults to
\code{OPENAI_ADMIN_KEY}.}

\item{limit}{Number of daily buckets requested per page, from 1 to 180.
Pagination is automatic.}

\item{base_url}{Base URL for the OpenAI API.}
}
\value{
A data frame with one row per cost result and columns:
\code{start_time}, \code{end_time}, \code{amount}, \code{currency}, \code{quantity}, \code{line_item},
\code{api_key_id}, \code{project_id}, and \code{project_name}. Grouping columns that were
not requested may contain \code{NA}.
}
\description{
Retrieves actual costs from OpenAI's organization Costs API. Results can be
filtered to API key or project IDs and grouped by API key, project, or line
item. All pages are retrieved automatically.
}
\details{
This endpoint requires an OpenAI Organization Admin API key. It does not
accept the ordinary project API key used for model requests. Organization
owners can create an Admin API key at
\url{https://platform.openai.com/settings/organization/admin-keys}.

\code{amount} is the amount charged in \code{currency}; it is not estimated from token
counts. Sum \code{amount} only within a single currency.

OpenAI does not currently document an API endpoint for remaining prepaid
credit. That balance remains available in the organization Billing page.
}
\examples{
\dontrun{
# Daily organization costs for the current month
costs <- openai_costs(
  start_time = as.Date(format(Sys.Date(), "\%Y-\%m-01")),
  group_by = "line_item"
)
sum(costs$amount)

# Costs incurred by one API key
openai_costs(
  start_time = Sys.Date() - 30,
  api_key_ids = "key_abc123",
  group_by = c("api_key_id", "line_item")
)
}

}
\seealso{
\href{https://platform.openai.com/docs/api-reference/usage/costs}{OpenAI Costs API}
}
