% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat-anthropic.R
\name{chat_anthropic_extended}
\alias{chat_anthropic_extended}
\title{Chat with Anthropic Models (Extended Thinking + Structured Output)}
\usage{
chat_anthropic_extended(
  system_prompt = NULL,
  params = NULL,
  model = NULL,
  cache = c("5m", "1h", "none"),
  api_args = list(),
  base_url = "https://api.anthropic.com/v1",
  beta_headers = character(),
  api_key = NULL,
  credentials = NULL,
  api_headers = character(),
  echo = NULL,
  thinking = NULL
)
}
\arguments{
\item{system_prompt}{A system prompt to set the behavior of the assistant.}

\item{params}{Common model parameters, usually created by \code{\link[ellmer:params]{ellmer::params()}}.
When \code{thinking = "enabled"}, must include \code{reasoning_tokens} to set the
thinking budget.}

\item{model}{The model to use for the chat (defaults to
\code{"claude-sonnet-4-5-20250929"}).}

\item{cache}{Anthropic prompt caching strategy. One of \code{"5m"}, \code{"1h"}, or
\code{"none"}.}

\item{api_args}{Additional arguments passed to the API.}

\item{base_url}{Base URL for the Anthropic API.}

\item{beta_headers}{Character vector of Anthropic beta feature headers.}

\item{api_key}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Use \code{credentials} instead.}

\item{credentials}{Override the default credentials. You generally should not
need this argument; instead set \code{ANTHROPIC_API_KEY} in \code{.Renviron}.}

\item{api_headers}{Additional HTTP headers.}

\item{echo}{Whether to echo the conversation to the console.}

\item{thinking}{Thinking mode. One of:
\itemize{
\item \code{NULL} (default): auto-detect. If \code{reasoning_tokens} is present in
\code{params}, thinking is automatically enabled. Otherwise, no thinking
(behaves like \code{\link[ellmer:chat_anthropic]{ellmer::chat_anthropic()}}).
\item \code{"enabled"}: extended thinking with explicit budget. Requires
\code{reasoning_tokens} in \code{params}.
\item \code{"adaptive"}: adaptive thinking. Only available on Claude Opus 4.6 and
Sonnet 4.6; errors for 4.5 model IDs.
}}
}
\value{
A \link[ellmer:Chat]{ellmer::Chat} object.
}
\description{
Creates a chat interface for Anthropic's Claude API with support for
extended thinking combined with structured output. Standard
\code{\link[ellmer:chat_anthropic]{ellmer::chat_anthropic()}} cannot combine thinking (\code{reasoning_tokens})
with structured output because Anthropic's API forbids \code{tool_choice} with
thinking enabled. This provider uses Anthropic's \code{output_config.format}
(JSON schema) instead of \code{tool_choice} when thinking is active, avoiding
the API conflict.

When \code{thinking} is \code{NULL} (the default), thinking is auto-detected from
\code{params}: if \code{reasoning_tokens} is present, thinking is enabled
automatically. Otherwise, the function behaves identically to
\code{\link[ellmer:chat_anthropic]{ellmer::chat_anthropic()}}.
}
\details{
\subsection{How it works}{

When \code{thinking} is active and structured output is requested (via
\verb{$chat_structured()} or \code{\link[=batch_chat_structured]{batch_chat_structured()}}),
this provider uses Anthropic's \code{output_config.format} with a JSON schema
instead of the usual \code{tool_choice} mechanism. The model produces a
\code{thinking} block (internal reasoning) followed by a \code{text} block containing
JSON that matches the requested schema. This avoids the API error:
\emph{"Thinking may not be enabled when tool_choice forces tool use."}

When thinking is not active (no \code{reasoning_tokens} in \code{params} and
\code{thinking = NULL}), the provider falls back to standard
\code{tool_choice}-based structured output (identical to \code{ellmer::chat_anthropic()}).
}

\subsection{Model compatibility}{
\itemize{
\item \code{thinking = "enabled"}: All Claude 4.5 models (Haiku, Sonnet, Opus) and
later. Requires \code{reasoning_tokens} in \code{params} to set the thinking budget.
\item \code{thinking = "adaptive"}: Claude Opus 4.6 and Sonnet 4.6 only.
}
}
}
\seealso{
\code{\link[ellmer:chat_anthropic]{ellmer::chat_anthropic()}}, \code{\link[=batch_chat_structured]{batch_chat_structured()}}

Other chatbots: 
\code{\link{chat_gemini_extended}()},
\code{\link{chat_groq_developer}()}
}
\concept{chatbots}
